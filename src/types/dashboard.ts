export type DispatchLocation = {
  lat: number | null;
  lng: number | null;
};

export type IncidentFormState = {
  clientName: string;
  clientPhone: string;
  clientLicensePlate: string;
  vehicleIssue: string;
  operatorNote: string;
};
